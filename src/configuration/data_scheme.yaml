!obj:TwoDSift.twodsifts_dataset.TwoDSiftData {
            filenames: %(path)s,
            y_val: %(y_val)s,            # a list
            cv: %(cv)s,                  # a list
            shuffle_seed: %(seed)i,
            middle: %(middle_path)s,     # if not empty unlabeled examples will be added
            middle_val: %(middle_val)s,  # a list
            extend_data: False,
            reduce_dimension_to_1D: True,
        }
